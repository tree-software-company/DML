number age = 27;
assert age > 0;

string name = "Szymon";
assert name != "";

// Plik z funkcjami matematycznymi
function add(a, b) {
    return a + b;
}

function multiply(a, b) {
    return a * b;
}

function square(x) {
    return multiply(x, x);
}

var PI = 3.14159;
